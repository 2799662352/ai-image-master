feat: base64 OOM crash fix + codex official alignment batch; bump v4.3.87

P0 crash fix (renderer/main V8 heap OOM from 10-40MB base64 model images):
- imageResources.ts: materialize data: URLs into Blob + blob: URL on arrival;
  stores/IPC/history never hold base64 strings again
- cos:enqueue-upload-bytes IPC (ArrayBuffer, 64MB cap) + local disk fallback
  to userData/generated-images with localPath in upload results
- history: reference images thumbnailed to 640px JPEG; >1MB data: entries
  scrubbed on save; serialized history writes prevent file truncation
- DonorCard: COS thumbs via imageMogr2 512px WebP, async blob decode

Codex official alignment:
- reference roots gate: whitelist <userData>/agent/uploads on send side
  (parity with fs IPC gate); stale ENOENT references skipped with
  attachmentSkipped notice instead of failing the whole send
- clientUserMessageId + text_elements write side; userMessage item/completed
  reconcile (codexReconcile metadata block on our message rows)
- collaborationMode Plan preset: experimentalApi capability, list RPC,
  turn/start passthrough, CollabModeToggle in composer (smoke-verified)
- AttachmentService cleanup is now reference-aware (never sweeps files still
  referenced by chat history; fail-safe on scan errors)

Unified @ mention popup (MentionsV2 parity): installed plugins + skills +
files in one flow; plugin mention items ride along with @token text,
deduped by path; skills insert official $name marker.

Tests: 71+ new tests across main/agent, agent-chat store, MentionInput,
parseUnifiedDiff, openAiChange, AttachmentService cleanup, cosUploadHotSwap.

Docs: docs/2026-07-09-v4.3.87-oom-fix-codex-alignment.md + two design specs.
