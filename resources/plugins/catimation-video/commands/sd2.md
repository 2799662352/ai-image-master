---
description: Seedance 2.0 提示词工程化 — sd2-pe 总兜底引擎,把任何需求/草稿重写为工程化视频提示词
---

载入并执行 **sd2-pe**(Seedance 2.0 Prompt Optimizer,用 Skill 工具加载 `sd2-pe`)。这是**视频与提示词的总起点和兜底**:不论用户给的是模糊想法、堆砌形容词的草稿,还是完整多模态 JSON,都先用 sd2-pe 走完它的工作流:

1. **Step 0** 需求不全 → 启发式提问补足八大要素(精准主体/动作细节/场景/光影/运镜/风格/画质/约束)。
2. **Step 1** 判定任务类型(多模态参考/编辑/延长/组合)与镜头规划(单镜/多镜)——统一高规格单轨,无「简单视频」降级路径。
3. **Step 2** 素材映射(`图片N`/`视频N`/`音频N`、`<主体N>` 绑定、identity-hard
   按用户需要选择大头+全身/多视图/其它确认资产；多套拿不准时询问；运行时兼容
   归一化 @Image/@图片 等别名)。
4. **Step 3** 仅在关键歧义时打断;非关键缺失自动补全并透明披露。
5. **Step 3.3/4** 落笔前主动检索核实 2–3 个真实影视参考候选给用户选(先复用 reference-receipts 知识库),再用 Skill 工具加载 `seedance-cinematic-format`(必载结构叶子,单镜快速任务也要),按 总体设定→镜头分镜→风格约束 三段内容 + 12 字段骨架输出结构化提示词 + 优化问题 + 相关原则,字段可短不可删。

产出工程化提示词后,再按需叠加衍生技法:`seedance-video-craft`(模型对齐/配额/爆款体检)、`storyboard-*`(逐镜画面打磨)、`director-orchestrator`(成套分镜的 13 维定位)。最终交 `catimation-video` 的 `generate_video` 出片。

诉求/草稿/JSON 在下方:

$ARGUMENTS
