# v4.4.1 发布说明

> 发布渠道：腾讯云 COS 热更新（`releases/latest.yml`）+ GitHub Release 兜底
> 基线版本：v4.4.0

## 概览

本版把 Seedance 提示词工程从“每次强制加载完整结构叶子”调整为轻重分流：
仅简单单镜连续任务走路径 A，可跳过 `seedance-cinematic-format`；复杂、多镜、
混合媒介或需展开导演/作品参考任一命中即走路径 B，再加载完整媒介 profile。
路径 B 条件优先于生成、编辑、延长、组合等任务类型。两条路径只在内容展开程度
和加载成本上不同，八大要素、12 项内容与五大必备块均不能缺失。

## 提示词结构

- 12 项从固定方括号字段改为内容覆盖清单；标题、顺序、空行和散文形式自由。
- 路径 B 覆盖总体设定、镜头流程、风格与约束三组语义，但不强制三段或固定标题。
- 八大核心要素全部必填；路径 A 可把场景、光影、运镜、风格、画质和约束压缩
  为短语或自动补全，但不能省略。
- 五大内容块保持必备：演出核心与递进、收束方向、品质绝对基准、详细高品质参考、
  监督演出思考。
- 百分比只表达语义收束方向，不作为 Seedance API 参数或可保证结果的模型权重。

## 素材引用与故事板

- Skill 写作使用 `@图片N / @视频N / @音频N`，应用在工具边界归一为
  `图片N / 视频N / 音频N`；两层语义不再混称。
- 长图、九宫格、故事板和电影美术设定板由用户在“拆格精确执行”与“整板低约束
  氛围参考”之间选择；用户要求直出时采用推荐默认，不阻塞流程。
- 整板参考继续强制“提示词主导 / 氛围板低约束”前缀，不冒充身份锚点或首帧。

## Codex CLI 0.144.5

- 内置 Codex CLI 从 0.144.4 升级到 0.144.5。
- 上游补丁扩展危险命令检测（含更多强制 `rm` 形式），并让命令被拒绝时的原因
  更清晰；本次没有 app-server 协议变更。
- Windows x64 主程序、sandbox setup 与 command runner 均按上游 SHA-256
  重新锁定，并继续使用完整生产参数集做离线握手冒烟。

## 商城版本

- 插件：`catimation-video` 1.0.23、`catimation-director` 1.0.14、
  `catimation-storyboard` 1.0.11、`catimation-storyboard-pro` 1.0.10、
  `catimation-film` 1.0.11、`catimation-core` 1.0.18。
- 独立 Skill：`sd2-pe` 1.0.27、`seedance-cinematic-format` 1.0.14、
  `director-orchestrator` 1.0.13、`seedance-video-craft` 1.0.15、
  `film-studio` 1.0.13、`storyboard-grid-to-seedance` 1.0.7。

## 验证

- Skill 架构审计：136 Skills / 36 hooks，0 违规。
- Skill 架构与契约测试：19/19。
- 首方 Skill 安装测试：29/29。
- first-party、top-level 与 Codex marketplace 镜像无漂移。
- `build:vite` 与 marketplace dry-run 通过；线上插件/Skill catalog 已核验。
