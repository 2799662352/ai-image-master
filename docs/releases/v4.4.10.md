# v4.4.10 发布说明

> 发布渠道:腾讯云 COS 热更新(`releases/latest.yml`)+ GitHub Release 兜底
> 基线版本:v4.4.9

## 概览

本版三条主线:**分镜切图/智能去字幕全面免密钥**——一个密钥都不填也能
直接用,结果转存历史桶永不过期;**Right.Codes 网关域名迁移**——原域名
被墙后内置预设切换到新域名,Codex 通道恢复可用;以及**界面补全**——
分镜切图 tab 回归导航,工作区查看器可单独收起。

## 免密钥通道(分镜切图 / 智能去字幕)

- **零配置可用(#104)**:未配置腾讯云密钥时,自动向 SCF 云函数请求
  `scope=media` 的 30 分钟临时票据(COS 仅 `smart-erase/*` +
  `storyboard-split/*` 前缀读写删 + MPS 四个 action,最小权限);
  永久密钥只存在云函数环境变量里,永不下发客户端。长任务轮询每轮
  换新票据,25 分钟以上的去字幕任务不断票。设置页显示
  「免密钥 · 云端临时授权」。
- **提交入口放行(#105)**:拆掉智能去字幕/分镜切图提交入口的
  「必须有永久密钥」旧门——免密钥模式下不再弹「未配置腾讯云密钥」。
- **畸形密钥自动降级(#105)**:误把别家 API key 粘进设置页
  (非 `AKID` 开头,实测导致 COS `InvalidAccessKeyId`)会被自动忽略
  并降级到免密钥通道;设置页新增「清除密钥」按钮 + 保存时 AKID
  格式校验。
- **结果永不过期(#105)**:任务完成后,去字幕视频/全部切片自动从
  媒体桶转存到历史记录页使用的公开读桶(`image-history/smart-erase/*`
  与 `image-history/storyboard-split/*` 前缀),历史记录保存**永久
  公网 URL**(界面显示「永久 / PERM」);此前免密钥模式下签名 URL
  只活 30 分钟。转存失败自动退回签名 URL,不影响任务成功。
- **端到端实证(#105)**:新增冒烟脚本 `scripts/smoke-sts-erase-e2e.ts`,
  用免密钥票据真提交去字幕任务(系统预设模板 303)全链路通过
  (上传 → 提交 → 处理 → 出片 → 清理)。

## Codex 通道

- **Right.Codes 域名迁移**:`right.codes` 被墙(官方维护公告
  2026-07-24),内置预设 codexProviders 与 gatewayModelRouting
  双通道(codex/grok)baseUrl 全部切换到 `rightapi.ai`,
  Codex 对话恢复可用。

## 界面

- **分镜切图 tab 回归(#103)**:导航栏「智能去字幕」旁恢复
  「分镜切图」入口,点击即达。
- **工作区查看器单独收起(#103)**:文件面板新增「只收中间查看器」
  模式——中间查看器列收起露出底下生图/生视频界面,左侧文件树保持
  可见可用;打开画布/文件等自动展开。

## 验证

- 主进程 tencent + smartErase 11 套件 98/98 全绿(新增
  historyBucketTransfer 4 例、畸形密钥降级 2 例、转存链路 2 例);
  smart-erase / storyboard-split 渲染层 18/18 全绿。
- 每个 PR 均通过 Quality Gate 七项检查(TypeScript / Vite Build /
  Contracts / Skill Architecture / 单测覆盖率 / Electron E2E)。
- 免密钥端到端冒烟 `SMOKE_E2E_ALL_PASS` 实跑通过(STS 票据 →
  COS 上传 → MPS 模板 303 去字幕 → 出片 → 清理)。
