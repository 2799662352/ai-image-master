# Release 2.2.1 打包发布 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将版本从 2.0.6 升级到 2.2.1，执行打包并校验产物。

**Architecture:** 按 docs/packaging-guide.md 标准流程：改版本号 → 跑测试 → build → 校验产物（安装包/latest.yml/skills 资源）→ 提交 → 推送。

**Tech Stack:** Electron-Vite, electron-builder (NSIS), Node.js

---

### Task 1: 升级版本号到 2.2.1

**Files:**
- Modify: `package.json:3`（version 字段）
- Modify: `docs/packaging-guide.md:29`（示例版本号）

**Step 1: 修改 package.json version**

将 `"version": "2.0.6"` 改为 `"version": "2.2.1"`

**Step 2: 修改 packaging-guide.md 示例版本**

将文档中 `"version": "2.0.6"` 改为 `"version": "2.2.1"`

**Step 3: 验证修改**

Run: `node -e "console.log(require('./package.json').version)"`
Expected: `2.2.1`

**Step 4: Commit**

```bash
git add package.json docs/packaging-guide.md
git commit -m "chore(release): bump version to 2.2.1"
```

---

### Task 2: 运行全部测试确认绿色

**Step 1: 运行所有相关测试**

Run: `npx vitest run`
Expected: ALL PASS

**Step 2: 如果有失败，先修复再继续**

---

### Task 3: 执行打包

**Step 1: 执行 build**

Run: `npm run build`
Expected: exit code 0

**Step 2: 检查产物**

1. 安装包存在：`release/catimation-cyberpunk-master-2.2.1-setup.exe`
2. latest.yml 版本正确：`release/latest.yml` 中 `version: 2.2.1`
3. skills 资源已打包：`release/win-unpacked/resources/skills/` 存在 `*.md`
4. 可执行文件存在：`release/win-unpacked/CATIMATION-Cyberpunk Master.exe`

**Step 3: Commit + Push**

```bash
git add package.json docs/packaging-guide.md
git commit -m "chore(release): 2.2.1 verified build"
git push origin main
```
