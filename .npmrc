# pnpm 默认不会把传递性 optionalDependencies 提到顶层 node_modules。
# electron-builder 打包时只扫顶层 node_modules,平台 prebuilt 子包(如
# @parcel/watcher-win32-x64)不在那 → 装到用户机器上启动就 crash。
# 解决:把 @parcel/watcher-* 强制 hoist 到顶层。
#
# 历史教训:v4.3.10 安装包就是因为缺这条 .npmrc 出现
#   "No prebuild or local build of @parcel/watcher found. Tried
#    @parcel/watcher-win32-x64." 启动崩溃,v4.3.11 才修复。
public-hoist-pattern[]=@parcel/watcher-*

# 同款根因第二次:v4.3.20 是第一个把 `import sharp from 'sharp'`(主进程
# media:thumb IPC 热路径,PR #22)真正发布出去的包,启动即崩
#   "Could not load the \"sharp\" module using the win32-x64 runtime"。
# sharp 的平台 prebuilt `@img/sharp-win32-x64`(内含 sharp-win32-x64.node)
# 是 transitive optionalDependency,pnpm 不 hoist → electron-builder 扫不到 →
# 没打进 app.asar.unpacked。强制 hoist sharp 本体 + 所有平台 @img/sharp-* 子包
# 到顶层,配合 electron-builder.yml 的 asarUnpack 规则一起生效。v4.3.21 修复。
public-hoist-pattern[]=sharp
public-hoist-pattern[]=@img/sharp-*
